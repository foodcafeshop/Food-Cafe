{
  "name": "food-cafe-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:merchant": "npm run dev -w apps/merchant",
    "dev:customer": "npm run dev -w apps/customer",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}